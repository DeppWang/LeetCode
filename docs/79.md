# [79. Word Search](https://leetcode-cn.com/problems/word-search/)

```
board =
[
  ['A','B','C','E'],
  ['S','F','C','S'],
  ['A','D','E','E']
]

Given word = "ABCCED", return true.
Given word = "SEE", return true.
Given word = "ABCB", return false.
```

### Thanking

- find the first chracter location.
- use "Queue"

### Code

```Python
import logging

logging.basicConfig(level=logging.INFO)


# Python3
class Solution:
    def exist(self, board, word: str) -> bool:
        def judge(first_char_index):
            logging.info("first_char_index: %s", first_char_index)
            queue = [first_char_index]
            logging.info("queue: %s", queue)
            word_index = 1
            for coordinate in queue:
                if word_index > len(word) - 1:
                    break
                x, y = coordinate[0], coordinate[1]
                key = word[word_index]
                logging.info('x: %s, y: %s, key: %s', x, y, key)
                if x - 1 >= 0 and board[x - 1][y] == key:
                    queue.append([x - 1, y])
                elif x + 1 < len(board) and board[x + 1][y] == key:
                    queue.append([x + 1, y])
                elif y - 1 >= 0 and board[x][y - 1] == key:
                    queue.append([x, y - 1])
                elif y + 1 < len(board[0]) and board[x][y + 1] == key:
                    queue.append([x, y + 1])
                else:
                    break
                word_index += 1
                logging.info('queue: %s', queue)
            return len(queue) == len(word)
        #
        # for i in range(len(board[0])):
        #     for j in range(len(board)):
        #         if board[i][j] == word[0]:
        #             return judge([i, j])
        # return False

        def get_first_char_indexs():
            indexs = []
            for i in range(len(board[0])):
                for j in range(len(board)):
                    if board[i][j] == word[0]:
                        indexs.append([i, j])

        indexs = get_first_char_indexs()
        index = indexs[0]
        if judge(index) is False:
            judge(index)
        for index in indexs:
            return judge(index) and

        # first_char_index = get_first_char()
        # logging.info("first_char_index: %s", first_char_index)
        # if first_char_index is None:
        #     return False
        # queue = [first_char_index]
        # logging.info("queue: %s", queue)
        # word_index = 1
        # for coordinate in queue:
        #     if word_index > len(word) - 1:
        #         break
        #     x, y = coordinate[0], coordinate[1]
        #     key = word[word_index]
        #     logging.info('x: %s, y: %s, key: %s', x, y, key)
        #     if x - 1 >= 0 and board[x - 1][y] == key:
        #         queue.append([x - 1, y])
        #     elif x + 1 < len(board) and board[x + 1][y] == key:
        #         queue.append([x + 1, y])
        #     elif y - 1 >= 0 and board[x][y - 1] == key:
        #         queue.append([x, y - 1])
        #     elif y + 1 < len(board[0]) and board[x][y + 1] == key:
        #         queue.append([x, y + 1])
        #     else:
        #         break
        #     word_index += 1
        #     logging.info('queue: %s', queue)
        # return len(queue) == len(word)


if __name__ == "__main__":
    solution = Solution()
    board = [["A", "B", "C", "E"], ["S", "F", "C", "S"], ["A", "D", "E", "E"]]
    word = "SEE"

    print(solution.exist(board, word))

```

